{
  "name": "@pyramid-systems/domain",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx tsc && npx jest --coverage",
    "test-watch": "concurrently -n typescript,jest -kill-others \"npx tsc --watch\" \"npx jest --watch --color\""
  },
  "author": "Pyramid Systems, Inc.",
  "license": "ISC",
  "dependencies": {
    "@aws/dynamodb-data-mapper-annotations": "^0.7.3",
    "aws-sdk": "^2.437.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "moduleNameMapper": {
      "^@pyramid-systems/core(.*)$": "<rootDir>../core$1",
      "^@pyramid-systems/domain(.*)$": "<rootDir>$1"
    },
    "modulePathIgnorePatterns": [
      "dist"
    ],
    "collectCoverage": true
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.7",
    "concurrently": "^4.1.0",
    "jest": "^24.7.1",
    "jest-junit": "^6.3.0",
    "jest-sonar-reporter": "^2.0.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.5"
  }
}
