{
  "name": "root",
  "version": "0.0.1",
  "description": "Project created by PAC",
  "devDependencies": {
    "lerna": "^3.15.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  },
  "scripts": {
    "install": "npx lerna bootstrap"
  },
  "husky": {
    "hooks": {
      "pre-push": "git stash --include-untracked && npx lerna run build && npx lerna run test && git stash pop"
    }
  },
  "author": "Pyramid Systems, Inc.",
  "license": "ISC"
}
